/**
 * Sample module for testing Continue + OpenRouter (Qwen3 Coder Agent).
 * Try: "Add a function that returns the nth Fibonacci number"
 * or "Add JSDoc and fix the types."
 */

export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function sum(a: number, b: number): number {
  return a + b;
}

// TODO: ask Continue to add more utilities or tests
